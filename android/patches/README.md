# Android patches

Place only reviewed, committed `*.patch` files in this directory. The builder
checks and applies every patch with `git apply`; a missing or non-applicable
patch is a hard failure. There are no Android-specific patches in the pinned
PoDoFo 1.1.0 baseline.
